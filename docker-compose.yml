version: '3.8'

services:
  web:
    build: .
    container_name: sentinelpay-app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - FLASK_ENV=production
    restart: unless-stopped
