# PostgreSQL Database Configuration
# Production / Render URL format:
# DATABASE_URL=postgresql://username:password@host:5432/database

# Local PostgreSQL Configuration (Optional if not using DATABASE_URL):
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=sentinelpay_db
